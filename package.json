{
  "name": "kitabu-digital",
  "version": "0.1.0",
  "private": true,
  "description": "Kitabu — offline-first property management for Kenyan landlords (monorepo)",
  "type": "module",
  "license": "UNLICENSED",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "node --disable-warning=ExperimentalWarning --test \"packages/core/test/*.test.ts\" \"apps/local-api/test/*.test.ts\"",
    "typecheck": "tsc -p tsconfig.json && tsc -p apps/local-api/tsconfig.json && tsc -p apps/web/tsconfig.json",
    "dev": "bash scripts/dev.sh",
    "demo": "npm run build -w apps/web && KITABU_STATIC=apps/web/dist node --disable-warning=ExperimentalWarning apps/local-api/src/main.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=22.18"
  }
}
